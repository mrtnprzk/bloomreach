import React from "react";

const FormSubmitButton = () => {
  return (
    <button type="submit" className="bg-yellow-400 rounded w-32 py-1">
      Apply filters
    </button>
  );
};

export default FormSubmitButton;
